import AuthForm from "../components/AuthForm";

export default function Home() {
  return (
    <div className="bg-[var(--gk-baseBackground)] min-h-screen flex items-center justify-center">
      <AuthForm />
    </div>
  );
}
